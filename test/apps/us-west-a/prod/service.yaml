apiVersion: v1
kind: Service
metadata:
  name: my-web-service
  annotations:
    my-cloud-label: 'my-prod-annotation'
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080